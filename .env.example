# SHA Claims Verification System — environment variables
# Copy to .env and fill in actual values. Never commit .env to version control.

DATABASE_URL=postgresql://sha_app_user:your_password_here@localhost:5432/sha_claims_db
JWT_SECRET=generate_a_long_random_secret_key_here
ENVIRONMENT=development
